---
title: Color List
---

<header class="site-header">
  <%= image_tag 'logo.svg', :class => 'site-header__logo' %>
  <p class="site-header__tagline">
    A collection of colors brought to you by <a target="_blank" href="http://samuellie.se">samuellie.se</a>.
    <br>
    Click a color you like to copy the HEX code.
  </p>
</header>

<main class="colors">
  <% sorted_colors = data.colorlist.colors.sort_by { |color| color.hex } %>
  <% sorted_colors.each do |color| %>
    <div
      class="
            colors__color
            <% if color.textColor %>
              colors__color--<%= color.textColor %>-text
            <% end %>"
      style="background-color:#<%= color.hex %>"
      data-color="<%= color.hex %>">
    </div>
  <% end %>
</main>
